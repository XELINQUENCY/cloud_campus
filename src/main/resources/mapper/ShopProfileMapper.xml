<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.ShopProfileMapper">

    <resultMap id="shopProfileResultMap" type="entity.shop.ShopProfile">
        <id property="userId" column="user_id"/>
        <result property="balanceShop" column="balance"/>
        <result property="points" column="points"/>
        <result property="passwordShop" column="payment_password"/>
    </resultMap>

    <select id="findById" resultMap="shopProfileResultMap">
        SELECT user_id, balance, points, payment_password
        FROM shop_profiles WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="entity.shop.ShopProfile">
        INSERT INTO shop_profiles (user_id, balance, points, payment_password)
        VALUES (#{userId}, #{balanceShop}, #{points}, #{passwordShop})
    </insert>

    <update id="update" parameterType="entity.shop.ShopProfile">
        UPDATE shop_profiles SET
        balance = #{balanceShop},
        points = #{points},
        payment_password = #{passwordShop}
        WHERE user_id = #{userId}
    </update>

    <update id="updateBalance">
        UPDATE shop_profiles SET balance = #{newBalance} WHERE user_id = #{userId}
    </update>

</mapper>