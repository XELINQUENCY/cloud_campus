<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.OrderItemMapper">

    <resultMap id="orderItemResultMap" type="entity.shop.OrderItem">
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="price" column="price"/>
    </resultMap>

    <select id="findByOrderId" resultMap="orderItemResultMap">
        SELECT order_id, product_id, quantity, price
        FROM order_items WHERE order_id = #{orderId}
    </select>

    <insert id="insert" parameterType="entity.shop.OrderItem">
        INSERT INTO order_items (order_id, product_id, quantity, price)
        VALUES (#{orderId}, #{productId}, #{quantity}, #{price})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO order_items (order_id, product_id, quantity, price)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.quantity}, #{item.price})
        </foreach>
    </insert>

</mapper>